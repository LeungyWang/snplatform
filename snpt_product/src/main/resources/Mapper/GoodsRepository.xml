<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wly.repository.GoodsRepository">


    <select id="findAllByUserId" resultType="Goods">
        select * from goods where applicant= #{param1} limit #{param2},#{param3}
    </select>

    <select id="findGoodByuserid" resultType="Goods">
        select * from goods where applicant= #{param1}
    </select>

    <select id="countByUserid" resultType="int">
        select count(id) from goods where applicant = #{applicant}
    </select>

    <select id="findById" parameterType="String" resultType="Goods">
        select  name, originplace,category,picture,quantity,unit,cost,price, status, applicant,applicationtime from goods where id = #{id}
    </select>

    <insert id="save" parameterType="Goods">
        insert into goods(id, name, originplace,category,picture,quantity,unit,cost,price, status, applicant,applicationtime) values (#{id},#{name},#{originplace},#{category},#{picture},#{quantity},#{unit},#{cost},#{price},#{status},#{applicant},#{applicationtime})
    </insert>

    <update id="update" parameterType="Goods">
        update goods set status = #{status} where id = #{id}
    </update>


    <delete id="deleteById" parameterType="String">
        delete from goods where id = #{id}
    </delete>


</mapper>